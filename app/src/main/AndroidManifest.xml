<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.masterhelper">

  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

  <application
    android:name=".global.GlobalApplication"
    android:allowBackup="true"
    android:debuggable="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/CustomTheme">
    <activity android:name=".goals.PageGoal">
    </activity>
    <activity android:name=".media.FileViewerWidget">
    </activity>
    <activity android:name=".locations.PageLocation">
    </activity>
    <activity android:name=".locations.PageLocationsList">
    </activity>
    <activity android:name=".goals.PageGoalsList">
    </activity>
    <activity android:name=".journeys.PageJourneyList">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
  </application>

</manifest>